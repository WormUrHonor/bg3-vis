import type { ClassFeatureModule } from "./classFeatureTypes";

const rangerFeatures = [];

export const rangerClassModule: ClassFeatureModule = {
  className: "Ranger",
  defaultTabLabel: "Spells & Ranger Features",
  subclassTabLabels: {},
  features: rangerFeatures,
  iconFileByFeatureId: {},
};
