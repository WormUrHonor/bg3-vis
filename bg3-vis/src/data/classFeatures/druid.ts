import type { ClassFeatureModule } from "./classFeatureTypes";

const druidFeatures = [];

export const druidClassModule: ClassFeatureModule = {
  className: "Druid",
  defaultTabLabel: "Spells & Wild Shape",
  subclassTabLabels: {},
  features: druidFeatures,
  iconFileByFeatureId: {},
};
